# Cloudtype은 빌드 마지막에 `COPY ./ ./`로 컨텍스트 전체를 /app에 넣습니다.
# 컨텍스트를 거의 비우지 않으면 이미지가 5GB를 넘습니다.
# Docker 문서 패턴: * 로 제외 후 필요한 파일만 허용
*
!streamlit_app.py
!gemini_file_search.py
!requirements-docker.txt
!requirements.txt
